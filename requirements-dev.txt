# Dependencias solo para desarrollo/CI -- NO se instalan en Render (los
# workflows de producción siguen usando solo requirements.txt). Instalar
# con: pip install -r requirements.txt -r requirements-dev.txt
pytest==9.1.1
